import Typography from '@libs/ui-kit/components/Typography';

const DateAndTime: React.FC = ({ children }) => (
  <Typography variant="h5" textAlign="right">{children}</Typography>
);

export default DateAndTime;
